<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rapport d'Analyse Mensuel</title>
    <style>
        /* Styles CSS ici */
        body {
            font-family: Arial, sans-serif;
            background-color: #f9f9f9;
            margin: 0;
            padding: 0;
            color: #333;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        header {
            text-align: center;
            margin-bottom: 20px;
        }

        h1 {
            color: #007bff;
            font-size: 28px;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .monthly-report {
            margin-bottom: 30px;
            border: 2px solid #007bff;
            border-radius: 8px;
            padding: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
        }

        th, td {
            padding: 10px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        th {
            background-color: #f2f2f2;
        }

        footer {
            text-align: center;
            margin-top: 20px;
            padding-top: 20px;
            border-top: 1px solid #ddd;
            color: #777;
            font-size: 14px;
        }

        footer p {
            margin: 0;
        }
    </style>
</head>
<body>
<div class="container">
    <header>
        <h1>Rapport d'Analyse Mensuel</h1>
    </header>
    <main>
        <section class="monthly-reports">
            {% for mois, analyses in analysesParMois %}
                <div class="monthly-report">
                    <h2>{{ mois }}</h2>
                    <table>
                        <thead>
                        <tr>
                            <th>Nom de l'analyse</th>
                            <th>Nombre de fois</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for nomAnalyse, nombre in analyses %}
                            <tr>
                                <td>{{ nomAnalyse }}</td>
                                <td>{{ nombre }}</td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            {% endfor %}
        </section>
        <section class="total-analysis">
            <p>Nombre total d'analyses effectuées : {{ totalAnalyses }}</p>
        </section>
    </main>
    <footer>
        <p>&copy; 2024 {{ labo.nom }}. Tous droits réservés.</p>
    </footer>
</div>
</body>
</html>
